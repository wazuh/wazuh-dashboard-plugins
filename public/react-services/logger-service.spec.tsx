import React from 'react';
import { expect } from 'chai';
import { loggerService } from './logger-services';

describe('Spec LoggerService', function () {
  it('it exists', () => {
    expect(loggerService).to.be.ok;
  });
});