/**
 * User-facing messages (centralized for i18n and consistency).
 */
export const MESSAGES = {
  WELCOME: 'Welcome!',
  ERROR_PARSING_JSON: 'Error parsing JSON query',
  INSUFFICIENT_PERMISSIONS: 'Insufficient permissions to execute this method',
  INSUFFICIENT_PERMISSIONS_SHORT: 'Forbidden: insufficient permissions',
} as const;
