const {
  generateSampleDataWithDataset,
  getDatasetIndex,
} = require('../../../plugins/main/server/lib/sample-data/index');

module.exports = {
  generateSampleDataWithDataset,
  getDatasetIndex,
};
