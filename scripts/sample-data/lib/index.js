const {
  generateSampleDataWithDataset,
  getDatasetIndex,
  getDatasetTemplate,
} = require('../../../plugins/main/server/lib/sample-data/index');

module.exports = {
  generateSampleDataWithDataset,
  getDatasetIndex,
  getDatasetTemplate,
};
