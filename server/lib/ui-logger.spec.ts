import React from 'react';
import { expect } from 'chai';
import { addUiLog } from './ui-logger';

describe('Spec UI Logger addUiLog', function () {
  it('it exists', () => {
    expect(addUiLog).to.be.ok;
  });
});