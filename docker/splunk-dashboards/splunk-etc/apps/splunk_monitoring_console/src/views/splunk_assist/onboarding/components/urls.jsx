import { createURL, createDocsURL } from '@splunk/splunk-utils/url';

export const connectivityBlockedUrl = createDocsURL('learnmore.assist.blockedSCSconnectivity');

export const fipsUrl = createDocsURL('learnmore.splunkassist.fips');

export const instrumentationUrl = createURL('manager/splunk_instrumentation/instrumentation_index');

export const instrumentationDocsUrl = createDocsURL('learnmore.splunkassist.instrumentation');

export const availabilityBestPracticesUrl = createDocsURL('learnmore.splunkassist.availabilitybestpractices');

export const cloudConnectedUrl = createDocsURL('learnmore.splunkassist.cloudconnected');

export const assistIntroUrl = createDocsURL('learnmore.splunkassist.intro');
