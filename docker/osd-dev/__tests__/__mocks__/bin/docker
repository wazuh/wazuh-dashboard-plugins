#!/usr/bin/env bash
log_file="${DOCKER_STUB_LOG:-}"
if [[ -n "$log_file" ]]; then
  printf '%s\n' "docker $*" >> "$log_file"
fi

if [[ $1 == "compose" ]]; then
  # Simulate docker compose; allow forcing failures if needed.
  exit ${DOCKER_COMPOSE_EXIT:-0}
fi

exit 0
