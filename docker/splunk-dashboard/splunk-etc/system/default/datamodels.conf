#   Version 9.0.3
# DO NOT EDIT THIS FILE!
# Changes to default files will be lost on update and are difficult to
# manage and support.
#
# Please make any changes to system defaults by overriding them in
# apps or $SPLUNK_HOME/etc/system/local  
# (See "Configuration file precedence" in the web documentation).
#
# To override a specific setting, copy the name of the stanza and
# setting to the file where you wish to override it.
#
# This file contains configurations for datamodels and their properties.
#

[default]
acceleration                    = false
acceleration.earliest_time      = 
acceleration.backfill_time      =
acceleration.max_time           = 3600
acceleration.allow_skew         = 0
acceleration.cron_schedule      = */5 * * * *
acceleration.schedule_priority  = default
acceleration.manual_rebuilds    = false
acceleration.poll_buckets_until_maxtime = false
acceleration.max_concurrent     = 3
acceleration.allow_old_summaries = false
acceleration.source_guid         =
acceleration.hunk.compression_codec  =
acceleration.hunk.dfs_block_size     =
acceleration.hunk.file_format        = 
acceleration.workload_pool           = 

## dataset related
dataset.description                         =
dataset.type                                = datamodel
dataset.commands                            =
dataset.fields                              =
dataset.display.diversity                   = latest
dataset.display.sample_ratio                = 1
dataset.display.limiting                    = 100000
dataset.display.currentCommand              =
dataset.display.mode                        = table
dataset.display.datasummary.earliestTime    =
dataset.display.datasummary.latestTime      =

strict_fields                                  = true
