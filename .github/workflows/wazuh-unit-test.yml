# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Wazuh Unit Tests

on:
  push:
    branches:
      - 'disable'
  pull_request:
    branches:
      - '*.*-*.*'

jobs:
  setup-wazuh-app:
    name: Run setup environment wazuh app
    runs-on: ubuntu-18.04
    env:
      NODE_10-23-1: '10.23.1'
      NODE_12-22-3: '12.22.3'
      NODE_14-17-4: '14.17.4'
      YARN_1-21-1: '1.21.1'
      KIBANA_TAG: 'v7.10.2'
    steps:
      - name: Step 01 - Download Project kibana
        uses: actions/checkout@v2
        with:
          repository: elastic/kibana
          ref: ${{ env.KIBANA_TAG }}
          path: kibana

      - name: Step 02 - Setup node for kibana app
        uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_10-23-1 }}

      - name: Step 03 - Install dependencies of kibana
        run: |
          cd kibana
          yarn set version ${{ env.YARN_1-21-1 }}
          yarn kbn bootstrap
          mkdir plugins/wazuh-kibana-app

      - name: Step 04 - Download Project wazuh-app
        uses: actions/checkout@v2
        with:
          path: kibana/plugins/wazuh-kibana-app

      - name: Step 05 - Setup node version for wazuh app
        uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_12-22-3 }}

      - name: Step 06 - Install dependencies of wazuh
        run: |
          cd kibana/plugins/wazuh-kibana-app/
          yarn install

      - name: Step 07 - Setup node for Jest
        uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_14-17-4 }}

      - name: Step 08 - Collect & comment coverage
        uses: artiomtr/jest-coverage-report-action@v2.0-rc.3
        with:
          working-directory: ./kibana/plugins/wazuh-kibana-app/
          github-token: ${{ secrets.GITHUB_TOKEN }}
          test-script: yarn run test:jest --silent --ci --testLocationInResults --colors --coverage --json --outputFile=report.json
          package-manager: yarn

# using old comments
#      - name: Step 09 - Comment Test Coverage
#        uses: AthleticNet/comment-test-coverage@1.2.2
#        with:
#          token: ${{ secrets.GITHUB_TOKEN }}
#          path: ./kibana/plugins/wazuh-kibana-app/target/test-coverage/coverage-summary.json
#          title: Global Test Coverage
#
#      - name: Step 10 - Run Tests & Collect Coverage only changed files
#        run: |
#          cd ./kibana/plugins/wazuh-kibana-app
#          yarn run test:jest --changedSince="origin/4.3-7.10" --colors
#
#      - name: Step 11 - Comment Test Coverage only changed files
#        uses: AthleticNet/comment-test-coverage@1.2.2
#        with:
#          token: ${{ secrets.GITHUB_TOKEN }}
#          path: ./kibana/plugins/wazuh-kibana-app/target/test-coverage/coverage-summary.json
#          title: Brach Test Coverage
